version: '3'

includes:
  library:
    taskfile: ../library/Ritefile.yml
    dir: ../library

tasks:
  start:
    cmds:
      - task: library:login
        vars:
          SERVER: fubar
          UNAME: fubar
      - task: library:login
        vars:
          SERVER: bar
          UNAME: bar
